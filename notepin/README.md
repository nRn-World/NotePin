# NotePin (extension source)

This folder contains the Vite + React source for the NotePin Chrome extension.

- Build: `npm run build`
- Output: `dist/` (Chrome → Extensions → Developer mode → Load unpacked → select `dist/`)

The main README lives in the repository root.